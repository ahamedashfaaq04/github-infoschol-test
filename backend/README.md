This folder contains backend-related static data for the simple quiz platform.

- `data/questions.json` — a static JSON file with general knowledge questions used by the frontend.

No server is required to run this demo; the frontend fetches `../backend/data/questions.json` when served over HTTP.
